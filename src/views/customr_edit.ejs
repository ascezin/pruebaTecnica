
<%- include('partials/header') %> 

<div class="container mt-5" >
    <div class="row">
        <div class="col-md-5">
            <div class="card">
                <div class="card-body">
                    <form action="/update/<%= data.id %>" method="post">
                        <div class="form-group">
                            <p class="badge bg-primary text-wrap" style="width: 100%;">Usuario: </p>
                            <input type="text" name="usuario" value="<%= data.usuario %>" placeholder="Usuario" class="form-control">
                        </div>
                        <div class="form-group">
                            <p class="badge bg-primary text-wrap" style="width: 100%;">Contraseña: </p>
                            <input type="text" name="contraseña" value="<%= data.contraseña %>" placeholder="Contraseña" class="form-control">
                        </div>
                        <div class="form-group">
                            <p class="badge bg-primary text-wrap" style="width: 100%;">Nombre: </p>
                            <input type="text" name="mombre" value="<%= data.mombre %>" placeholder="Nombre" class="form-control">
                        </div>
                        <div class="form-group">
                            <p class="badge bg-primary text-wrap" style="width: 100%;">Apellido: </p>
                            <input type="text" name="apellido" value="<%= data.apellido %>" placeholder="Apellido" class="form-control">
                        </div>
                        <div class="form-group">
                            <p class="badge bg-primary text-wrap" style="width: 100%;">RolID: </p>
                            <input type="int" name="id_rol" value="<%= data.id_rol %>" placeholder="rol ID" class="form-control">
                        </div>
                        <div class="form-group">
                            <p class="badge bg-primary text-wrap" style="width: 100%;">estado: </p>
                            <input type="text" name="estado" value="<%= data.estado %>" placeholder="Telefono" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-success">
                            Editar Usuario
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %> 